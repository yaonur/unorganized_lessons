mod handler;
mod model;
mod route;
mod schema;

use std::sync::Arc;

use axum::http::{

};
use sqlx::{postgres::PgPoolOptions, Pool, postgres};
#[tokio::main]
async fn main() {
    let pi = 3.14;
    println!("Hello, world!");
    let i = 22;
}
